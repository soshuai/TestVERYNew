<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"><!-- 其中rectagle表示矩形，oval表示椭圆，line表示水平直线，ring表示环形 -->

    <corners android:radius="200dp" />

    <solid android:color="@color/blue" />

    <stroke
        android:width="1dp"
        android:color="@color/black" />

    <padding
        android:bottom="3dp"
        android:left="12dp"
        android:right="12dp"
        android:top="3dp" />

    <!-- 节点2： gradient (背景颜色渐变)
           android:startColor 起始颜色
           android:centerColor 中间颜色
           android:endColor 末尾颜色
           android:angle 渐变角度，必须为45的整数倍。
           android:type 渐变模式 默认是linear(线性渐变)radial(环形渐变)
           android:centerX X坐标
           android:centerY Y坐标
           android:gradientRadius radial(环形渐变)时需指定半径
       -->
    <gradient
        android:type="radial"
        android:gradientRadius="200dp"
        android:startColor="@color/blue"
        android:centerColor="@color/red"
        android:endColor="@color/black"/>
</shape>